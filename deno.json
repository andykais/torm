{
  "name": "@andykais/torm",
  "version": "0.1.0",

  "tasks": {
    "test":       "deno test --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net --import-map=test/import_map.json test",
    "test:watch": "deno test --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net --import-map=test/import_map.json --watch"
  },

  "lint": {
    "rules": {
      "exclude": ["no-explicit-any", "no-unused-vars"]
    }
  },

  "exports": {
    ".": "./src/mod.ts",
    "./drivers/sqlite": "./drivers/sqlite.ts",
  }
}

{
  "tasks": {
    "test": "deno test --unstable --check --allow-read --allow-write --allow-ffi --allow-env=HOME --allow-net test",
    "test:watch": "deno test --watch --unstable --check --allow-read --allow-write --allow-ffi --allow-env=HOME test"
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  }
}
